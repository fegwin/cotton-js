import { Keyboard, KEYDOWN, KEYUP, PRESSED, RELEASED } from "./keyboard";
import { CLICK, Mouse, MOVE } from "./mouse";
import { Swipe, SWIPEDOWN, SWIPEIDLE, SWIPELEFT, SWIPERIGHT, SWIPEUP } from "./swipe";

export const input = {
  CLICK,
  KEYDOWN,
  KEYUP,
  MOVE,
  PRESSED,
  RELEASED,
  SWIPEDOWN,
  SWIPEIDLE,
  SWIPELEFT,
  SWIPERIGHT,
  SWIPEUP,

  Keyboard,
  Mouse,
  Swipe,
};
