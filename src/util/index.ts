import { loadImage } from "./image";
import { loadJson } from "./json";
import {
  BoundingBox,
  Circle,
  getRandomInt,
  getRandomNumber,
  Polygon,
  Vector2,
} from "./math";

export const util = {
  BoundingBox,
  Circle,
  Polygon,
  Vector2,
  getRandomInt,
  getRandomNumber,
  loadImage,
  loadJson,
};
